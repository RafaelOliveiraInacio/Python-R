##Principais Funcoes
##Head e Tail

esoph ##Base teste

##Defaut 6 Linhas
head(esoph) ## Cabeçalho

tail(esoph) ## Ultimas Linhas

##10 Linhas
head(esoph, n=10) ## 10 primeiras linhas

##Dimensoes
dim(esoph) ## Numero de linhas e Colunas

dim(esoph)[1] ##Acessar primeiro conteudo do vetor

##Comprimento
length(islands) ##retorna o tamanho

##Nome das colunas

colnames(esoph) ##colunas
rownames(esoph) ##linhas

summary(esoph) ##Resumo dos dados de forma categorica, retornando o balanceamento de cada categoria

##Escolher arquivos

x = file.choose()
x








